

def join_data(local_source, table_name, destination):
    '''
    Join_data is to join bases of the same format with same columns values
        local_source is a string - put the address of the databases
        table_name is a string - write the new name of the clustered data base
        destination is a string - enter the address you want to save new database
    '''
    name = pd.DataFrame()
    for f in glob.glob(local_source + "/*.xlsx"):
        df = pd.read_excel(f)
        name = name.append(df, ignore_index = True)
    name.to_excel( destination + "\ " + table_name + ".xlsx", index = None, header = True)
    return name.head()
